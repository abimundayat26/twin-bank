import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function BalancePage() {
  return (
    <PagePlaceholder
      title="Balance Trajectory"
      purpose="The projection behind the last simulation: its bands, markers and assumptions."
    />
  );
}
